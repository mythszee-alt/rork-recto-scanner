# Tasks - Bypass Authentication

## Authentication Gating
- [x] Modify `AppViewModel.kt` to default to `LIBRARY` gate.
- [x] Update `completeOnboarding` in `AppViewModel.kt`.
- [x] Enable `GOOGLE_SIGN_IN_ENABLED` in `GateScreens.kt`.
- [x] (Optional) Ensure Paywall is bypassable or skipped.

## Verification
- [ ] Verify onboarding flow leads to Library.
- [ ] Verify sign-in is accessible from Account screen.
